name: TITAN V PRO Wolt Path Validation

on:
  push:
    paths:
      - 'bugbounty_runner/bridge/wolt-path-validation-command.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install runtime
        run: python -m pip install --disable-pip-version-check requests
      - name: Run minimal Wolt root canonicalization validation
        run: python bugbounty_runner/validate_wolt_path_norm.py
      - uses: actions/upload-artifact@v4
        with:
          name: titan-wolt-path-validation
          path: wolt-path-validation.json
          retention-days: 2
