name: BugBounty AI Multi-Program 8-Runner Scanner
on:
  workflow_dispatch:
    inputs:
      config_path:
        description: 'Reviewed multi-program HackerOne scope config JSON'
        required: true
        default: 'bugbounty_runner/config.example.json'
  push:
    paths:
      - 'bugbounty_runner/bridge/command.json'
permissions:
  contents: read
jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      config_path: ${{ steps.resolve.outputs.config_path }}
      command_id: ${{ steps.resolve.outputs.command_id }}
    steps:
      - uses: actions/checkout@v4
      - id: resolve
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            CONFIG='${{ inputs.config_path }}'
            COMMAND_ID="manual-${{ github.run_id }}"
          else
            CONFIG=$(python3 - <<'PY'
          import json
          d=json.load(open('bugbounty_runner/bridge/command.json'))
          assert d.get('bridge_name') == 'HUNTER8'
          assert d.get('action') == 'scan'
          print(d['config_path'])
          PY
          )
            COMMAND_ID=$(python3 - <<'PY'
          import json
          d=json.load(open('bugbounty_runner/bridge/command.json'))
          print(d['command_id'])
          PY
          )
          fi
          test -f "$CONFIG"
          echo "config_path=$CONFIG" >> "$GITHUB_OUTPUT"
          echo "command_id=$COMMAND_ID" >> "$GITHUB_OUTPUT"
          echo "HUNTER8 command=$COMMAND_ID config=$CONFIG"

  scan:
    needs: prepare
    strategy:
      fail-fast: false
      max-parallel: 8
      matrix:
        runner: [0,1,2,3,4,5,6,7]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install runtime
        run: pip install requests
      - name: Run scanner pack on all reviewed programs and domains
        run: |
          mkdir -p results
          echo "HUNTER8 ${{ needs.prepare.outputs.command_id }} runner=${{ matrix.runner }} starting"
          python bugbounty_runner/multi_scan.py \
            --config '${{ needs.prepare.outputs.config_path }}' \
            --runner '${{ matrix.runner }}' \
            --out 'results/runner-${{ matrix.runner }}.json'
          echo "HUNTER8 runner=${{ matrix.runner }} complete"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: multi-result-${{ matrix.runner }}
          path: results/runner-${{ matrix.runner }}.json
          retention-days: 2

  analyze:
    needs: [prepare, scan]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: multi-result-*
          path: results
          merge-multiple: true
      - name: Aggregate, deduplicate and rank candidates
        run: |
          echo "HUNTER8 ${{ needs.prepare.outputs.command_id }} analyzing"
          python bugbounty_runner/analyze.py | tee results/summary.txt
      - uses: actions/upload-artifact@v4
        with:
          name: multi-program-combined-analysis
          path: results/
          retention-days: 2
